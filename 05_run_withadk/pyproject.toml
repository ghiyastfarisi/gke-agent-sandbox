[project]
name = "run-withadk"
version = "0.1.0"
description = "An ADK agent writes Python; a gVisor sandbox on GKE runs it."
requires-python = ">=3.12"
dependencies = [
    "google-adk[gke]>=2.6.0",
    "k8s-agent-sandbox>=0.5.4",
    "python-dotenv>=1.0.0",
]
